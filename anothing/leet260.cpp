#include<vector>
#include<set>
#include<unordered_set>
#include<unordered_map>
#include<queue>
#include<stack>
#include<iostream>
#include<algorithm>
#include<cmath>
#include <numeric>  // 为了使用std::accumulate()函数 
#include<sstream>
// #include"TreeNode.h"
using namespace std;
// vector<int> singleNumber(vector<int>& nums) {
//     int x = 0;
//     for(auto & it : nums){
//         x ^= it;
//     }
//     int lowbit = x & -x;
//     //int ans[2];// C++ 中，普通的数组不支持返回，因为它们被分配在栈上，
// //而函数返回后它们将被销毁。
//     vector<int> ans(2);
//     for(auto& it : nums){
//         if((it & lowbit))ans[0] ^= it;
//         else ans[1] ^= it;
//     }
//     return ans;
// }
int main(){
    unsigned int a = -1;
    cout << a;
}