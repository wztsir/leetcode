#include<vector>
#include<set>
#include<map>
#include<unordered_set>
#include<unordered_map>
#include<queue>
#include<stack>
#include<iostream>
#include<algorithm>
#include<cmath>
#include<cstring>
#include<numeric> // 为了使用std::accumulate()函数 
#include<sstream>
#include<functional>
using namespace std;
int maximumSum(vector<int>& arr) {
    int begin = -1, n = arr.size();
    bool flag = false;
    vector<int> temp(arr);
    for(int i = 1; i < n; i++){
        if(ans < 0){
            ans = max(ans, arr[i]);
        }else{
            if(arr[])
            ans = max(ans, arr[i] + ans);
            flag = true;
        }
    }
}
int main()
{
   system("pause");
   return 0;
}
